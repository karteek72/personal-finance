# Deprecated — use the single repo-root environment file.
#   cp .env.example .env   (from repository root)
#
# Podman compose loads ../.env automatically (see compose.yaml).
# Optional deploy overrides (ports, public URLs): containers/deploy.env.example
